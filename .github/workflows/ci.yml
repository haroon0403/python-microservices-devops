name: CI/CD Pipeline

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.0403456 }}
          password: ${{ secrets.dckr_pat_G_iEuxHnrfx1gULSM-KwEzGSntU }}

      - name: Build and Push Backend
        run: docker build -t ${{ secrets.0403456 }}/backend:latest ./backend && docker push ${{ secrets.0403456 }}/backend:latest

      - name: Build and Push Frontend
        run: docker build -t ${{ secrets.0403456 }}/frontend:latest ./frontend && docker push ${{ secrets.0403456 }}/frontend:latest

      - name: Build and Push Logger
        run: docker build -t ${{ secrets.0403456 }}/logger:latest ./logger && docker push ${{ secrets.0403456 }}/logger:latest

